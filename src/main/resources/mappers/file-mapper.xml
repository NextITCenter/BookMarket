<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.nextit.bookmarket.board.FileMapper">
    <insert id="saveFiles" parameterType="list">
        INSERT INTO
            ATTACHED_FILE
            (BOARD_NO, FILE_PATH, FILE_NAME, ORIGINAL_NAME, FILE_SIZE)
        <foreach collection="list" item="f" separator="UNION ALL">
        SELECT
            #{f.boardNo}, #{f.filePath}, #{f.fileName}, #{f.originalName}, #{f.fileSize}
        FROM
            DUAL
        </foreach>
    </insert>
    <select id="selectFile" parameterType="int" resultType="fileVO">
        SELECT
            ID,
            BOARD_NO,
            FILE_PATH,
            FILE_NAME,
            ORIGINAL_NAME,
            FILE_SIZE,
            REGISTER_DATE
        FROM
            ATTACHED_FILE
        WHERE
            ID = #{id}
    </select>
</mapper>




